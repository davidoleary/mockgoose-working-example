# mockgoose-working-example
Mockgoose working for tests in watch mode

When running tests pass in an ENV variable to the mocked database to be used instead of the real one.

```
npm install
npm run test-watch
```
